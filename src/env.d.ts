
/// <reference types="vite/client" />

// IMPORTANT: This project does not use environment variables for database configuration.
// Instead, it uses localStorage to store Supabase credentials entered by the user.
// This makes the application portable and compatible with multiple deployments.

